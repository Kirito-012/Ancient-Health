export const metadata = {
  title: 'My Profile | Ancient Health',
  robots: { index: false, follow: false },
}

import Profile from '../../views/Profile'

export default function ProfilePage() {
  return <Profile />
}
