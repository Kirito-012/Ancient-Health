export const metadata = {
  title: 'Checkout | Ancient Health',
  robots: { index: false, follow: false },
}

import Checkout from '../../views/Checkout'

export default function CheckoutPage() {
  return <Checkout />
}
