export const metadata = {
  title: 'Your Cart | Ancient Health',
  robots: { index: false, follow: false },
}

import Cart from '../../views/Cart'

export default function CartPage() {
  return <Cart />
}
