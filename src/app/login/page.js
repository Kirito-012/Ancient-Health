export const metadata = {
  title: 'Login | Ancient Health',
  robots: { index: false, follow: false },
}

import Login from '../../views/Login'

export default function LoginPage() {
  return <Login />
}
